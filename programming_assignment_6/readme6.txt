first make sure have modules are downloaded.

I use IDlE edit this project. 

To run the program:
just open command prompt to where the .py file is located,
then type python hw6a.py in command line.
it will pump out 4 images of the plot, after close each one of them,
the result will appear in the command prompt.

 
have tested K-means algorithm with iris-dataset

1st image is after testing iris-dataset with k-means,
2nd image is after testing iris-dataset with clusters and centroids
3rd image is after testing hierarchy
4th image is elbow method








#X_pca=pca_fit_transform
#pca for iris